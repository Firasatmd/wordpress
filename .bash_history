ll
cd DOCKER-WORDPRESS
ll
docker-compose version
sudo vi Docker-compose.yaml
docker-compose up -d
ll
Docker-compose up -d
docker compose up -d
docker ps
docker images
ll
vi 
cat 
ll
sudo yum install docker -y
sudo service  docker start
sudo usermod -aG docker ec2-user
sudo curl -L
sudo curl -L sudo curl -L https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m) -o /usr/local/bin/docker-compose
sudo curl -L https://github.com/docker/compose/releases/download/1.22.0/docker-compose-$(uname -s)-$(uname -m) -o /usr/local/bin/docker-compose
sudo chmod +x/usr/local/bin/docker-compose
sudo chmod+X/usr/local/bin/docker-compose
sudo chmod +X/usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
docker-compose version
mkdir DOCKER-WORDPRESS
cd DOCKER-WORDPRESS
ll
sudo vi docker-compose.yaml
ll
cd DOCKER-WORDPRESS
ll
vi Docker-compose.yaml
docker compose up -d
docker compose up --d
docker-compose up -d
ll
rm  Docker-compose.yaml~
ll
Docker-compose up -d
docker-compose up -d
ll
sudo vi docker-compose.yaml
ll
vi Docker-compose.yaml
ll
rm Docker-compose.yaml
ll
sudo vi docker-compose.yaml
docker-compose up -d
ll
vi docker-compose .yaml
ll
vi docker-compose.yaml
docker-compose up -d
vi docker-compose.yaml
docker-compose up -d
ll
cd DOCKER-WORDPRESS
ll
rm docker-compose.yaml~
ll
vi docker-compose.yaml
docker-compose up -d
vi docker-compose.yaml
docker-compose up -d
vi docker-compose.yaml
docker-compose up -d
vi docker-compose.yaml
docker-compose up -d
vi docker-compose.yaml
docker-compose up -d
docker-compose version
cat docker-compose.yaml
ll
vi docker-compose.yaml
docker-compose up -d
ll
cd DOCKER-WORDPRESS
ll
vi docker-compose.yaml
docker-compose up -d
vi docker-compose.yaml
docker-compose up -d
vi docker-compose.yaml
docker-compose up -d
vi docker-compose.yaml
docker-compose up -d
vi docker-compose.yaml
docker-compose up -d
vi docker-compose.yaml
docker-compose up -d
docker ps
docker images
vi docker-compose.yaml
